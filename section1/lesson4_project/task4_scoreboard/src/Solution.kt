/**
 * Участник чемпионата: имя и очки. Готовый data class, менять не нужно.
 */
data class Participant(val name: String, val points: Int)

/**
 * Собери таблицу участников в текст: "<место>. <имя> — <очки>" по одной
 * строке на участника, разделённых "\n". Порядок — по очкам по убыванию,
 * при равенстве — по имени по возрастанию. Для пустого списка верни "".
 */
fun formatScoreboard(participants: List<Participant>): String = TODO("реализуй")
